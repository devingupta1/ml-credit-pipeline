"""
predictor.py

Loads the serialized model artifact and feature pipeline at startup.
Provides a predict() interface that accepts raw application features,
applies the transformation pipeline, and returns calibrated default
probabilities.
"""

pass
